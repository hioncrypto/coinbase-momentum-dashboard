# Core
streamlit>=1.37
pandas>=2.2
numpy>=1.26,<2.0
requests>=2.32
websocket-client>=1.7
python-dateutil>=2.9
pytz>=2024.1
tenacity>=8.2

# Display / images (pin to wheel so it won't compile)
pillow==10.4.0

# Optional: speed up JSON/arrow where available, but keep versions safe
pyarrow<17
